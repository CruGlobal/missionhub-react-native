import { StyleSheet } from 'react-native';

import theme from '../../theme';

export default StyleSheet.create({
  detailsWrap: {
    marginTop: 1,
  },
  detailText: {
    fontSize: 14,
    color: theme.grey1,
  },
  detailTextRed: {
    fontSize: 14,
    color: theme.red,
  },
});
