import { StyleSheet } from 'react-native';

import theme from '../../theme';

export default StyleSheet.create({
  pathwayStageNotSet: {
    color: theme.white,
  },
  iconPathwayStageNotSet: {
    backgroundColor: theme.red,
  },
  emailButton: {
    fontSize: 20,
  },
});
