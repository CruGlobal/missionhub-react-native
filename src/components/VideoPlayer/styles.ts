import { StyleSheet } from 'react-native';

// import theme from '../../theme';

export default StyleSheet.create({
  videoContainer: {
    width: '100%',
    height: 300,
  },
  videoPlayer: {
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,
  },
});
