import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  videoContainer: {
    width: '100%',
    height: 300,
  },
  videoPlayer: {
    ...StyleSheet.absoluteFillObject,
  },
});
