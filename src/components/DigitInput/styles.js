import { StyleSheet } from 'react-native';

import theme from '../../theme';

export default StyleSheet.create({
  digit: {
    fontSize: 36,
    color: theme.white,
    textAlign: 'center',
    width: 36,
    margin: 4,
  },
});
