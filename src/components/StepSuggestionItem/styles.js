import { StyleSheet } from 'react-native';

import theme from '../../theme';

export default StyleSheet.create({
  card: {
    marginHorizontal: 20,
    marginVertical: 4,
    padding: 20,
  },
  stepText: {
    fontSize: 16,
    lineHeight: 22,
  },
});
