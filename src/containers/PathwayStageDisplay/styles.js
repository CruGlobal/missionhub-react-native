import { StyleSheet } from 'react-native';

import theme from '../../theme';

export default StyleSheet.create({
  stage: {
    color: theme.white,
    fontSize: 14,
    letterSpacing: 0.25,
  },
});
